export const SECTION = Object.freeze({
  NEW_UPLOAD: 'new-upload',
  RECENT: 'recent'
})

export const FILES_RETRIEVAL_MODE = Object.freeze({
  RECENT: 'recent',
  ALL: 'all'
})

export const STATUS = Object.freeze({
  IDLE: 'idle',
  SELECTED: 'selected',
  PENDING: 'pending',
  SUCCESS: 'success',
  FAILED: 'failed'
})
